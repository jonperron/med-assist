# Copy to .env before running `docker compose up`.
#   cp .env.example .env
#
# Nothing here is a secret: the service stores nothing and authenticates
# nobody, so there is no datastore to protect and no key to hold. Every value
# below tunes inference, bounds a container, says which build to serve, or -
# UNSECURED_DEPLOYMENT - decides whether the interface warns the person using
# it that this installation is open to anyone.

# Where the NER weights are on this machine. They are mounted read-only at
# /app/models rather than copied into the image, so a retrained model is
# picked up by `docker compose restart app` with no rebuild - the app and
# interface share one container now, so that restart bounces the browser's
# connection too, briefly. The directory holds the files `from_pretrained`
# reads - config.json, model.safetensors, tokenizer.json, tokenizer_config.json.
#
# Write it as an absolute path, or as a relative one starting with `./`. Compose
# reads a bare `models` as the name of a Docker volume rather than as a
# directory, and it does not expand `~` - `~/models` creates a directory
# literally named `~` beside this file. A relative path resolves against the
# directory holding docker-compose.yml, not against your shell's.
#
# Every one of those mistakes fails the same quiet way: a path that does not
# exist is created empty by Docker rather than refused, so the API starts,
# fails the load, and answers 503 on /readyz and on the analysis routes. The
# interface reports that as the service being unavailable and says no more,
# because it says nothing about configuration to a clinician. If that warning is
# on screen, check this path first, then `docker compose logs app`.
#
# The same 503-with-no-reason also follows from ownership rather than a bad
# path: both processes in the container run as uid 1001, not root, so files
# that are `chmod 600` and owned by your own account are unreadable to it.
# Either make the directory group- or world-readable, or add
# `user: "$(id -u):$(id -g)"` to the `app` service in docker-compose.yml.
#
# Point it only at a directory holding the weights, owned by whoever runs the
# deployment: whatever it names is mounted into the container's filesystem.
MODEL_DIR=./backend/models

# Threads torch may use for one inference. Left unset, docker-compose.yml
# defaults it to APP_CPU_LIMIT below, because torch reads the host's core
# count rather than the container's CPU quota and would otherwise start a
# thread per host core inside a three-core allowance. Shipped here one below
# APP_CPU_LIMIT rather than matching it, deliberately: the interface's Node
# process shares that same quota now and needs a core torch isn't holding.
# Set it to 0 to get torch's own default back, or lower to leave the host
# responsive.
NER_INFERENCE_THREADS=2

# How many documents may be inside the model at once. One by default: a second
# set of activations is what puts a small machine into swap. Above one, the same
# model object is called from several threads, which transformers does not
# document as safe - prefer running more replicas of this container instead
# (each carries its own idle interface process now, which is wasted work but
# not an unsafe one - see the coexistence cost in
# openwiki/decisions/2026-09-08-compose-builds-the-unified-image.md). If you do
# raise it, pin NER_INFERENCE_THREADS at the same time, or each inference
# claims every core and they contend.
NER_MAX_CONCURRENT_INFERENCES=1

# Set to `development` only to expose the mock endpoints. Never in a deployment.
APP_ENV=production

# Which browser origins may call the API, comma-separated, each written as
# scheme://host[:port] with no trailing slash and no port the scheme already
# implies - both are dropped rather than refused, since a browser omits them.
# This is the other half of NEXT_PUBLIC_API_URL below: that one tells the
# browser where the API is, this one tells the API which page is allowed to ask.
# This list is enforced on the server, not only advertised to browsers. A
# request to the analysis routes carrying an `Origin` that is not on it is
# refused with 403 before the body is read, so another site cannot drive an
# upload through a visitor's browser and spend this deployment's compute on
# documents of its choosing. CORS alone did not stop that: it withholds the
# answer from the attacking page, but the request was still analysed.
#
# The list is not the whole of the check, and an operator who narrows it will
# want the other two branches. A browser that reports `Sec-Fetch-Site:
# same-origin` or `none` is accepted before the list is consulted - that is
# what makes the one-domain proxy shape in `deploy/` work without you listing
# your own origin, and it means emptying this list does not turn the interface
# off. A request carrying no `Origin` but a `Sec-Fetch-Site` naming any other
# site is refused. A request carrying neither header is let through, because a
# proxy in front sends neither and neither does the healthcheck.
#
# It is not authentication, and nothing here is. A client that is not a browser
# writes whatever it likes in an `Origin` header, or omits both headers and
# lands in the branch that lets it through. Anyone who can reach the API can
# use it. `deploy/README.md` has the rest.
#
# Serve the interface from anywhere other than the default and both have to
# move, or the browser drops every answer. Left unset or removed from this
# file, `docker-compose.yml` still supplies its own default of
# `http://localhost:3050`, matching the frontend's published port below -
# rather than widening. Only a bare `uv run` outside Compose falls back
# further, to the backend's own built-in default of `http://localhost:3000`,
# which this file never reaches. The API answers with credentials, and a
# browser refuses a credentialed response allowed to `*` anyway, so the
# wildcard is rejected at startup instead of quietly breaking every call.
CORS_ALLOWED_ORIGINS=http://localhost:3050

# Which host address the API's port 8050 is published on. The default is
# loopback, so the published port answers the machine running the stack and
# nothing else. That matters, because `POST /api/analyze` ingests clinical
# documents and asks the caller for nothing: publishing the port on a public
# interface exposes it to everyone who can scan for it.
#
# It binds the published port and only that. Anything on the Docker network
# reaches the container directly whatever this is set to - a reverse proxy
# included - so a proxy you have pointed at the API, or a platform that gives
# the service a public domain of its own, reaches it with this on loopback.
# Loopback is one control among several, not the whole of who can reach the
# API; `deploy/README.md` has the rest.
#
# Set it to 0.0.0.0 only if you have decided the API should answer every address
# this host has, and read the lines above again before you do. It does not
# affect the interface's port 3000, which is published on all interfaces as
# before.
BACKEND_BIND_ADDRESS=127.0.0.1

# Puts a warning banner on every screen of the interface: this installation is
# open, anyone can reach it, and documents sent through it may be read by a
# third party. Set it to `true` on any deployment that someone other than you
# can reach - which is every deployment that is not your own machine.
#
# It is read at request time rather than baked into the frontend bundle, so
# turning it on is this variable and `docker compose restart app`. No rebuild.
#
# Unset and blank leave it off - that is the local default. Once set, only
# `0`, `false`, `no` and `off` turn it off; every other value turns it ON,
# including a misspelling. That asymmetry is deliberate: setting this variable
# at all means you have decided the deployment is public, and a typo must not
# be the reason a clinician never sees the warning.
#
# The banner is not a control. It changes what a clinician does, not what the
# service accepts - there is nothing in front of the API, by design. Read
# deploy/README.md before publishing this anywhere.
UNSECURED_DEPLOYMENT=

# What the one container may take from the host. It runs both processes, so
# this covers the model held resident by the API and the interface's Node
# process together. Measured with the API alone: 470 MiB idle, 504 MiB peak
# across a batch of five long documents; it starts and analyses at 512m with
# nothing to spare. The default here adds roughly the frontend's own former
# 512m allowance on top of four times that peak, because a large PDF is
# expanded in memory by its parser and the RAM-backed /tmp that uploads spool
# into is charged against this limit too.
APP_MEMORY_LIMIT=2.5g

# Inference is CPU-bound and this is the setting that decides how long a batch
# takes. Keep NER_INFERENCE_THREADS in step with it: measured on a 14-core host
# under a 2-core quota, five long documents took 11s with the two matched and
# 215s with torch left to pick its own thread count. The extra core over that
# measurement is the interface's former separate allowance, now folded in here
# since both processes share one quota.
APP_CPU_LIMIT=3

# The URL the browser uses to reach the API. Baked into the frontend bundle
# at build time, so changing it requires a rebuild of the whole image - 8050
# to match the host port above, not the container-internal 8000 the API
# actually listens on.
NEXT_PUBLIC_API_URL=http://localhost:8050
